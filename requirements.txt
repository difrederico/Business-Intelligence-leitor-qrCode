# Dependências principais
streamlit
pillow
pandas
opencv-python-headless
# Observação: o app fornece fallback para captura de câmera via
# `st.camera_input`, que funciona na maioria dos ambientes (incl. Streamlit Cloud).
# Se você precisa do streaming contínuo via WebRTC, adicione `streamlit-webrtc`
# manualmente ao requirements e assegure que o host ofereça suporte a WebRTC/ffmpeg.